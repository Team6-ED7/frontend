export const H2title = ({ className, children }) => {
  return (
    <h2
      className={`text-black font-rajdhani text-4xl font-black ${className} `}
    >
      {children}
    </h2>
  );
};
export default H2title;
